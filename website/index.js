import React from 'react';
import ReactDOM from 'react-dom';
import ApolloProvider from './apollo/Provider';

ReactDOM.render(<ApolloProvider>new app</ApolloProvider>, document.getElementById('root'));
